package main

import "fmt"

// Scaffold generated by create-orcaarch.
// Runs `make reports` or `make api` to auto-download the engine into .orcaarch/source.
func main() {
	fmt.Println("OrcaArch — {{PROJECT_NAME}}")
	fmt.Println("Scenario : logistics")
	fmt.Println("Volume   : {{VOLUME}} records")
	fmt.Println()
	fmt.Println("Pipeline:")
	fmt.Println("  mock.Generate({{VOLUME}})")
	fmt.Println("  → Reconcile      ERP vs carrier invoice")
	fmt.Println("  → Landed Cost    freight + duties + insurance")
	fmt.Println("  → WAC            weighted average cost per SKU")
	fmt.Println("  → Ledger         double-entry per reconciliation status")
	fmt.Println("  → Quarantine     invoices without ERP match")
	fmt.Println("  → Reprocess      reversal + recalculation on correction")
	fmt.Println("  → Reports        CSV + read-only API")
	fmt.Println()
	fmt.Println("Run `make help` to see available commands.")
	fmt.Println("Run `make reports` to generate CSV reports in ./output.")
	fmt.Println("Run `make api` to start the read-only API on localhost:8080.")
	fmt.Println("The full engine is downloaded into .orcaarch/source when needed.")
}
